package org.jetlinks.supports.ipc;

public enum RequestType {
    noArgFireAndForget,
    fireAndForget,
    noArgRequest,
    request,
    noArgRequestStream,
    requestStream,
    requestChannel,
    cancel
}

